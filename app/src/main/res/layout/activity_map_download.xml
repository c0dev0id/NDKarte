<?xml version="1.0" encoding="utf-8"?>
<!--
  Map Download screen.
  Layer order (bottom → top):
    1. InteractiveMapView  – full-screen map
    2. downloadsOverlay    – left-side slide-in download panel
    3. legend              – bottom-right status key
    4. tvTooltip           – centre-screen region name tooltip
    5. btnBack             – top-left back button (topmost)
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. Full-screen interactive map -->
    <com.ndkarte.app.InteractiveMapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 2. Left-side download overlay panel -->
    <FrameLayout
        android:id="@+id/downloadsOverlay"
        android:layout_width="250dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#CC000000"
        android:visibility="gone">

        <ScrollView
            android:id="@+id/downloadsScroll"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/downloadsPanel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:paddingStart="4dp"
                android:paddingEnd="4dp" />

        </ScrollView>

    </FrameLayout>

    <!-- 3. Bottom-right legend -->
    <LinearLayout
        android:id="@+id/legend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="8dp"
        android:background="#CC000000"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- Available -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <View
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:background="#2A4F6E" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Available"
                android:textColor="#FFFFFF"
                android:textSize="10sp"
                android:layout_marginStart="6dp" />

        </LinearLayout>

        <!-- Downloading -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <View
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:background="#1565C0" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Downloading"
                android:textColor="#FFFFFF"
                android:textSize="10sp"
                android:layout_marginStart="6dp" />

        </LinearLayout>

        <!-- Downloaded -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:background="#0D47A1" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Downloaded"
                android:textColor="#FFFFFF"
                android:textSize="10sp"
                android:layout_marginStart="6dp" />

        </LinearLayout>

    </LinearLayout>

    <!-- 4. Centre-screen tooltip (shown by Activity when a region is tapped) -->
    <TextView
        android:id="@+id/tvTooltip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="#CC000000"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:visibility="gone" />

    <!-- 5. Back button – top-left, rendered last so it sits above everything -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:background="#CC000000"
        android:text="←"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:padding="0dp" />

</FrameLayout>
